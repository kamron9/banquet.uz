const SectionTitle = ({title}:{title:string}) => {
	return (
		<div className='text-center mt-5'>
			<span className='text-primary sm:text-2xl'>{title}</span>
		</div>
	)
}

export default SectionTitle
